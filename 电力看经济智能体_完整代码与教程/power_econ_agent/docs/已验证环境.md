# 已验证环境与复现记录

验证日期：2026-07-13。

## 核心环境

- Linux x86_64
- Python 3.13.5
- NumPy 2.3.5
- pandas 2.2.3
- scikit-learn 1.8.0
- PyTorch 2.10.0 CPU
- Pydantic 2.13.4
- FastAPI 0.128.2
- Uvicorn 0.48.0
- Typer 0.26.3
- HTTPX 0.28.1
- NetworkX 3.6.1
- pytest 9.0.2
- Ruff 0.15.21

项目在 `pyproject.toml` 中使用兼容版本范围，不要求与以上补丁版本完全一致；以上版本仅用于记录本交付包的实际验证环境。

## 已通过的检查

```bash
python -m pytest -q
python -m compileall -q src tests scripts examples
python -m ruff check .
python scripts/verify_all.py --config configs/demo.yaml
```

验收结果：

- 17,544 行小时级数据；
- 39 个模型特征；
- 5 个显式特征组；
- 24 小时预测正常；
- 30 个测试期代表性事件；
- 诊断、报告、问答、SQLite 和 FastAPI 主要接口全部通过；
- 本地 CSV 四表适配器已用 `data/input/demo_split/` 完成 17,544 行数据与特征构建验证；
- wheel 构建和隔离目录导入通过。

详细机器可读结果：`artifacts/outputs/verification_summary.json`。

## 外部可选集成

默认演示完全离线。Open-Meteo、AKShare、OpenAI 和 MCP 属于可选集成，其代码、配置和错误处理已经包含；运行时还需要相应网络环境，并按文档安装对应 extra。外部服务变更、网络策略和数据可用性不属于离线验收范围。
